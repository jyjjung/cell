'use client';

import { InboxSheet } from '@/components/inbox/InboxSheet';

/** Community-wide inbox — same sheet on every app (em., preschool, account, etc.). */
export function AppInboxSheet() {
  return <InboxSheet />;
}
