
// This file is no longer used and can be deleted.
// The Bible reading plan generation is now handled locally by /src/lib/plan-generator.ts
// and configured via /src/components/admin/bible-plan-admin-form.tsx.

// Keeping the file temporarily to avoid breaking imports if any linger,
// but its content is irrelevant. It should be removed in a cleanup step.
export {}; // Ensures it's treated as a module
